# Rebuilt inside the image
node_modules
dist
dist-server
coverage

# Never belongs in a build context
.env
.env.*
!.env.example
.git
.github

# Editor / OS noise
.vscode
.idea
.DS_Store

# Local notes and docs — the build does not read them
*.md
