export function DiscoverRoute() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Discover</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Search and filter across all sources. Filters live in the URL.
      </p>
    </section>
  );
}
